a simple tweeter,
The terminal version has these features:
writing new tweet, like tweets, leave comments
Recently i've added a UI written by PyQT5, which is doesn't have the comment feature yet. but the there is the button and other stuff for it...
